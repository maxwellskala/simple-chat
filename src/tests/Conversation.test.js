import React from 'react';

import { wrap } from './utils';

describe('<Conversation />', () => {
  xit('renders one UserMessage per entry in props.message', () => {
    return;
  });

  xit('renders UserMessages on the LEFT if they are from another user', () => {
    return;
  });

  xit('renders UserMessages on the RIGHT if they are from the logged-in user', () => {
    return;
  });
});
